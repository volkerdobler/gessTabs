# Sample content — replace before release

This is a placeholder `release-notes/0.99.9.md` so you can see how the panel renders. Feel free to overwrite it with the real notes for this version, or delete it if 0.99.9 doesn't need a Release Notes popup at all.

## New in this version

- Autocomplete now works correctly right after typing `#`, even for a single character like `#z`.
- Fixed a **word-detection bug** that made a single-character prefix (e.g. `z`) show every suggestion, filtered or not.
- New: this very Release Notes panel — see the checkbox below.

## Things to note

- The checkbox at the bottom starts checked, so closing this panel now suppresses it for version `0.99.9` going forward.
- Uncheck it if you'd rather see this panel again on every start until you're done reading it.
- You can always reopen it later via **GESStabs: Show Release Notes** in the Command Palette.

Formatting check: this line has `inline code`, a [link](https://help.gessgroup.de), and both **bold** and plain text mixed together.
